(()=>{function apply(scope=document){scope.querySelectorAll('table').forEach(table=>{const headers=[...table.querySelectorAll('th')];if(headers.some(cell=>cell.textContent.includes('商品编码 / SKU ID'))){headers.filter(cell=>cell.textContent.trim()==='商品名称').forEach(cell=>cell.textContent='编码名称')}})}new MutationObserver(()=>apply()).observe(document.body,{childList:true,subtree:true});apply()})();
